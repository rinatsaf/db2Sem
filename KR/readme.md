# 1
## Никакие из созданных индексов не помогают они индексируют поля которые не используються 

## поэтому создаем индекс на id по которому у нас фильтрация

# 2 
## Никакие из созданных индексов не помогают они индексируют поля которые не используються 

## поэтому создаем индекс на id по которому у нас JOIN и индекс на уровень мембера gin так как там строки 

## преобладание shared hit значит что мы берем данные из кешэ 
## преобладание read значит что мы берем с диска.

# 3 
## 1. xmin теперь номер транзакции update, delete изменил xmax на транзакцию которая ее удалила
## 2.после update у старой строки поменялся xmax(помечено как мертвая) и создалась новая запись (update = delete + insert)
## 3. delete изменил xmax на транзакцию которая ее удалила
## 4.
-vacuum находит мертвые строки и помечает место которое они занимали как свободное
-autovacuum это vacuum и analyxe в фоне 
-full vacuum очищает мертвые строки уплотняет записи и отдает освобожденное место ОС 
## 5. Full vacuum может полностью блокировать бд
# 4 
## в Б Update ждет ролбека транзакции из A, выполняет делит только после него
## 